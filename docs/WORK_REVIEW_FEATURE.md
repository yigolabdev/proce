# Work Review & Approval Feature

## 📋 개요

작업자가 Work Input에서 작업 결과를 제출하면, 프로젝트 리더 및 팀 멤버가 확인하고 승인/거부할 수 있는 워크플로우를 구현했습니다.

## ✅ 구현된 기능

### 1. **확장된 Work Entry 타입**

```typescript
export interface WorkEntry {
  // 기본 필드
  id: string
  title: string
  description: string
  category: string
  date: Date | string
  duration: string
  projectId?: string
  objectiveId?: string
  tags?: string[]
  files?: FileAttachment[]
  links?: LinkResource[]
  
  // 승인 워크플로우 필드 (신규)
  status?: 'draft' | 'submitted' | 'pending_approval' | 'approved' | 'rejected'
  submittedBy?: string
  submittedAt?: Date | string
  reviewedBy?: string
  reviewedAt?: Date | string
  reviewComments?: string
}
```

### 2. **Work Review 페이지**

#### 경로
- `/app/work-review`

#### 주요 기능

##### a) 통계 대시보드
```
┌─────────────────────────────────────────────────────────┐
│  📊 Statistics                                          │
├───────────────────────────────────────────────────────┤
│  ⏱️ Pending: 5    📄 Submitted: 3    ✅ Approved: 12  │
│  ❌ Rejected: 2   📈 Total: 22                        │
└─────────────────────────────────────────────────────────┘
```

##### b) 상태별 필터링
- **All**: 모든 작업 항목
- **Pending Review**: 승인 대기 중
- **Submitted**: 제출됨
- **Approved**: 승인됨
- **Rejected**: 거부됨

##### c) 작업 항목 카드
각 작업 항목은 다음 정보를 표시:
- 제목 및 상태 배지
- 제출자 및 제출일
- 작업 시간
- 연결된 프로젝트/OKR
- 설명 및 태그
- 첨부 파일
- 관련 링크
- 리뷰 피드백 (있을 경우)

##### d) 리뷰 모달
작업을 클릭하면 상세 리뷰 모달이 표시:

```
┌─────────────────────────────────────┐
│  Review Work Submission            │
├─────────────────────────────────────┤
│  📋 Entry Info                      │
│  • Submitted by: John Doe          │
│  • Date: Nov 17, 2024              │
│  • Duration: 4h 30m                │
│  • Category: Development           │
│                                    │
│  📝 Description                     │
│  [Work description]                │
│                                    │
│  💬 Review Comments                │
│  [Text area for feedback]          │
│                                    │
│  ⚠️ Feedback will be shared        │
│                                    │
│  Actions:                          │
│  ✅ Approve  | 💬 Request Changes | ❌ Reject
└─────────────────────────────────────┘
```

### 3. **리뷰 액션**

#### ✅ Approve (승인)
- 작업을 승인하고 상태를 `approved`로 변경
- 리뷰어 정보 및 시간 기록
- 선택적으로 승인 코멘트 추가 가능

#### 💬 Request Changes (수정 요청)
- 작업자에게 수정 요청
- 상태를 `submitted`로 되돌림
- **필수**: 피드백 코멘트 작성
- 작업자가 수정 후 재제출 가능

#### ❌ Reject (거부)
- 작업을 거부하고 상태를 `rejected`로 변경
- **필수**: 거부 사유 작성
- 리뷰어 정보 및 시간 기록

### 4. **워크플로우**

```
┌──────────────┐
│   Draft      │ 작업자가 초안 작성
└──────┬───────┘
       │ Submit
       ▼
┌──────────────┐
│  Submitted   │ 제출됨
└──────┬───────┘
       │ Send for Review
       ▼
┌──────────────────┐
│ Pending Approval │ 리뷰 대기
└──────┬───────────┘
       │
       ├────── Approve ────────► ┌──────────┐
       │                         │ Approved │ 승인됨
       │                         └──────────┘
       │
       ├── Request Changes ────► ┌────────────┐
       │                         │ Submitted  │ 수정 후 재제출
       │                         └────────────┘
       │
       └────── Reject ──────────► ┌──────────┐
                                  │ Rejected │ 거부됨
                                  └──────────┘
```

## 🎨 UI/UX 디자인

### 상태 배지 색상 시스템

| 상태 | 색상 | 아이콘 | 의미 |
|------|------|--------|------|
| Pending Review | 🟡 Yellow | ⏱️ Clock | 리뷰 대기 중 |
| Submitted | 🔵 Blue | 📄 FileText | 제출됨 |
| Approved | 🟢 Green | ✅ CheckCircle2 | 승인됨 |
| Rejected | 🔴 Red | ❌ XCircle | 거부됨 |

### 통계 카드

각 통계 카드는 그래디언트 배경과 적절한 아이콘으로 시각화:

- **Pending**: 노란색 그래디언트
- **Submitted**: 파란색 그래디언트
- **Approved**: 초록색 그래디언트
- **Rejected**: 빨간색 그래디언트
- **Total**: 회색 그래디언트

## 📂 파일 구조

```
src/
├── app/
│   └── work-review/
│       └── page.tsx          # Work Review 메인 페이지
├── types/
│   └── common.types.ts       # 확장된 WorkEntry 타입
├── components/
│   └── layout/
│       └── AppLayout.tsx     # 네비게이션에 Work Review 추가
└── providers/
    └── AppProviders.tsx      # 라우팅 설정
```

## 🔧 사용 방법

### 사용자 관점

1. **작업 제출**
   - Work Input에서 작업 내용 작성
   - "Submit for Review" 버튼 클릭
   - 상태가 `pending_approval`로 변경

2. **리뷰 확인**
   - 사이드바에서 "Work Review" 메뉴 클릭
   - Pending Review 필터로 대기 중인 작업 확인

3. **작업 검토**
   - 작업 항목 클릭하여 상세 정보 확인
   - "Review" 버튼 클릭하여 리뷰 모달 열기
   - 피드백 작성 (필요 시)
   - Approve, Request Changes, 또는 Reject 선택

4. **피드백 확인**
   - 작업자는 Work History에서 리뷰 피드백 확인
   - 필요 시 수정 후 재제출

### 개발자 관점

#### Work Entry 생성 시

```typescript
const newWorkEntry: WorkEntry = {
  id: '...',
  title: 'Implement User Authentication',
  description: '...',
  status: 'submitted', // 또는 'pending_approval'
  submittedBy: 'John Doe',
  submittedAt: new Date(),
  // ... 기타 필드
}
```

#### 승인 처리

```typescript
const handleApprove = (entryId: string) => {
  const updated = entries.map((e) =>
    e.id === entryId
      ? {
          ...e,
          status: 'approved',
          reviewedBy: 'Current User',
          reviewedAt: new Date(),
          reviewComments: reviewComment || 'Approved',
        }
      : e
  )
  storage.set('workEntries', updated)
  toast.success('Work entry approved!')
}
```

## 💡 활용 시나리오

### 시나리오 1: 신규 기능 개발 완료
1. 개발자가 "Implement Payment Gateway" 작업 완료
2. Work Input에서 작업 내용, 코드 링크, 테스트 결과 첨부
3. "Submit for Review" 클릭
4. 프로젝트 리더가 Work Review에서 확인
5. 코드 품질, 테스트 커버리지 검토 후 승인

### 시나리오 2: 수정 요청
1. 디자이너가 "UI Mockup for Dashboard" 작업 제출
2. 프로덕트 매니저가 검토
3. "Request Changes" 선택하고 구체적인 피드백 제공
   - "Please update color scheme to match brand guidelines"
   - "Add mobile responsive mockups"
4. 디자이너가 피드백 확인 후 수정
5. 재제출하여 승인 받음

### 시나리오 3: 거부
1. 작업자가 불완전한 작업 제출
2. 리더가 검토 후 거부
3. 거부 사유 명시:
   - "Missing test cases"
   - "Does not meet acceptance criteria"
4. 작업자가 처음부터 다시 작성

## 🚀 향후 개선 방향

### 단기 (Phase 2)
- [ ] 이메일 알림 (작업 제출, 승인, 거부 시)
- [ ] 슬랙 통합 (알림 전송)
- [ ] 리뷰어 지정 기능
- [ ] 다중 리뷰어 지원

### 중기 (Phase 3)
- [ ] 리뷰 히스토리 추적
- [ ] 리뷰 템플릿
- [ ] 자동 승인 규칙 (특정 조건 만족 시)
- [ ] 리뷰 통계 및 리포트

### 장기 (Phase 4)
- [ ] AI 기반 작업 품질 평가
- [ ] 리뷰 시간 예측
- [ ] 팀별 리뷰 프로세스 커스터마이징
- [ ] 모바일 앱 지원

## 🧪 테스트 가이드

### 테스트 시나리오

1. **작업 제출**
   ```
   ✓ 작업 생성 후 상태가 'submitted'인지 확인
   ✓ submittedBy, submittedAt 필드가 올바르게 설정되는지 확인
   ```

2. **필터링**
   ```
   ✓ 상태별 필터가 정확하게 작동하는지 확인
   ✓ 통계 숫자가 올바른지 확인
   ```

3. **승인**
   ```
   ✓ 승인 후 상태가 'approved'로 변경되는지 확인
   ✓ reviewedBy, reviewedAt 필드가 설정되는지 확인
   ✓ 승인 토스트 메시지가 표시되는지 확인
   ```

4. **거부**
   ```
   ✓ 코멘트 없이 거부 시 에러 메시지 표시
   ✓ 거부 후 상태가 'rejected'로 변경
   ✓ 리뷰 코멘트가 저장되는지 확인
   ```

5. **수정 요청**
   ```
   ✓ 코멘트 없이 수정 요청 시 에러 메시지
   ✓ 수정 요청 후 상태가 'submitted'로 되돌아가는지 확인
   ✓ 피드백이 저장되는지 확인
   ```

## 📖 API 준비사항

향후 백엔드 통합을 위한 API 엔드포인트:

```typescript
// GET /api/work-entries?status=pending_approval
// 리뷰 대기 중인 작업 목록 조회

// POST /api/work-entries/:id/approve
// 작업 승인
{
  "reviewComment": "Great work!",
  "reviewedBy": "user-id"
}

// POST /api/work-entries/:id/reject
// 작업 거부
{
  "reviewComment": "Please address the following issues...",
  "reviewedBy": "user-id"
}

// POST /api/work-entries/:id/request-changes
// 수정 요청
{
  "reviewComment": "Please make the following changes...",
  "reviewedBy": "user-id"
}

// GET /api/work-entries/:id/review-history
// 리뷰 히스토리 조회
```

## 🎯 성공 지표

- ✅ 평균 리뷰 응답 시간 < 24시간
- ✅ 승인율 > 70%
- ✅ 수정 후 재승인율 > 90%
- ✅ 사용자 만족도 > 4.0/5.0

---

**구현 완료일**: 2024-11-17  
**담당자**: AI Assistant  
**상태**: ✅ 완료 (localStorage 기반)  
**다음 단계**: 백엔드 API 통합 준비

